SELECT COUNT(customer_id) AS customers_count
FROM customers;
--Query selects the count of customer IDs from the "customers" table and assigns it the alias "customers_count". It calculates the total number of customers present in the "customers" table.